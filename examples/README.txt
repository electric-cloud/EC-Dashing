This folder is intended to hold multiple example of widgets to dislay data
from ElectricCommander.

buildInformation:
	This widget is a push widget (meaning Commander sends the data to Dashing. It simply display information about a "specific build"
